{#
/**
 * Dashboard NG - Base Layout Template
 *
 * This template provides the base structure for all dashboard pages.
 * Note: The full HTML structure is already provided by GLPI's
 * Html::header() and Html::footer() - this template handles the
 * page content between them.
 */
#}

{# Navigation Bar #}
{% include 'components/navigation_bar.twig' with { navigation: navigation } %}

{# Page-specific content block #}
{% block content %}{% endblock %}

{# Optional JavaScript block #}
{% block javascripts %}{% endblock %}
